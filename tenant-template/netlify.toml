# Netlify configuration for tenant-template
# Runtime site for real tenants (NOT demo)

[build]
  command = ""
  publish = "public"
  functions = "functions"

[functions]
  directory = "functions"
  node_bundler = "esbuild"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
